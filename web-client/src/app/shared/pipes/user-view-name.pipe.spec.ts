import { UserViewNamePipe } from './user-view-name.pipe';

describe('UserViewNamePipe', () => {
  it('create an instance', () => {
    const pipe = new UserViewNamePipe();
    expect(pipe).toBeTruthy();
  });
});
