import { SetOrderStatusLabelPipe } from './set-order-status-label.pipe';

describe('SetOrderStatusLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SetOrderStatusLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
